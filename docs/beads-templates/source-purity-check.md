# Проверить чистоту источников в data_rework/

## Type
task

## Priority
P1

## Description
Создать и запустить проверку того, что в каждом каталоге `data_rework/{SOURCE}/` находятся только данные с соответствующим source.

## Критерий выполнения
- [ ] Создан скрипт `scripts/validation/check_source_purity.py`
- [ ] Скрипт добавлен в `scripts/validation/run-all.py`
- [ ] Проверка запущена для всех источников
- [ ] Все ошибки исправлены (если есть)
- [ ] Задача закрыта

## Context

**Related Sessions:**
- 2026-01-18: Исправлена структура classes.json (дубликаты, связывание подклассов)
- 2026-01-18: Обнаружена необходимость проверки чистоты источников

**Related Problems:**
- Риск: данные могут быть перемешаны между источниками
- Риск: cross-source ссылки могут быть сломаны
- Проблема: нет автоматической проверки корректности фильтрации

**Related Files:**
- `scripts/validation/check_source_purity.py` - новый скрипт (создать)
- `scripts/validation/run-all.py` - добавить новую проверку
- `data_rework/*/data/*.json` - файлы для проверки
- `docs/data-rework-structure.md` - документация структуры

**Предыстория (Background):**
После реорганизации данных из `/data/` в `data_rework/` по источникам, необходимо убедиться, что фильтрация работает корректно. Каждый каталог `data_rework/{SOURCE}/` должен содержать только сущности с `source: "{SOURCE}"`. Это критически важно для:
1. Целостности данных
2. Правильной работы cross-source ссылок
3. Предотвращения утечки данных между версиями (2014 vs 2024)

## План выполнения

1. Создать `scripts/validation/check_source_purity.py`
2. Добавить в `scripts/validation/run-all.py`
3. Запустить полную реорганизацию данных
4. Запустить проверку: `python scripts/validation/check_source_purity.py`
5. Исправить найденные ошибки (если есть)
6. Повторно запустить проверку
7. Закрыть задачу

## Технические детали

### Что проверять:
- Все JSON файлы в `data_rework/{SOURCE}/data/`
- Все сущности (классы, подклассы, существа, заклинания, предметы и т.д.)
- Вложенные сущности (subclasses в classes, features в subclasses)
- Поле `source` у каждой сущности

### Ожидаемый результат:
```
Проверка: data_rework
============================================================
✅ Проверка прошла успешно!
Проверено файлов: 44
```

### Если есть ошибки:
```
❌ Найдено 5 ошибок:
  📄 data_rework/PHB/data/classes.json: 2 ошибок
    ❌ Bard: PHB → XPHB
    ❌ College of Lore: PHB → XPHB
  ...
```

## Проверки

### После создания скрипта
```bash
python scripts/validation/check_source_purity.py
```

### После добавления в run-all.py
```bash
python scripts/validation/run-all.py
```

### Ожидаемый результат
- Статус: OK
- Ошибок: 0

---

**Создано:** 2026-01-18
**Автор:** Claude (по запросу пользователя)
